spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mystudy
    username: root
    password: 123456

  # 其他请求方法需要打开
  mvc:
    static-path-pattern: /mm/**

  hiddenmethod:
    filter:
      enabled: true